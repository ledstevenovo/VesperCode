[project]
name = "vesper-fixture"
version = "0.1.0"
description = "Reference fixture for the python-src-py312-v1 profile"
requires-python = ">=3.12,<3.13"

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
line-length = 88

[tool.mypy]
python_version = "3.12"
strict = True
