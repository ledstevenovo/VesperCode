# Reflection

## 2026-08-08 — 磁盘膨胀事件（Docker WSL 虚拟盘 78GB）

**事件**：C 盘被 Docker Desktop 的 WSL2 虚拟盘（`docker_data.vhdx`）撑满至 0 字节，Docker daemon 崩溃，用户其他软件写入失败。最终删除 vhdx（78GB）恢复 82GB 可用空间。

**技术教训**：
- Docker WSL 虚拟盘是**动态扩展且只增不减**——删除内容只释放盘内空间，文件本身不收缩；Windows 侧占用停留在峰值，必须显式 compact 才能回收。
- **悬空卷是最隐蔽的大头**：容器执行探针（registry/隔离/executor）每容器产生匿名卷，容器删除后卷残留——1261 个卷累积 64.5GB，远超镜像（3.2GB）与构建缓存（9.8GB）。任何"每容器一写"的探针都必须把卷清理纳入 cleanup 契约。
- 磁盘紧张时继续构建循环是灾难放大器：11GB 可用时仍进行了 7 轮构建，vhdx 重新膨胀。

**过程教训**：
- 构建的磁盘影响必须在第一次构建前向用户说明（"虚拟盘只增不减，需要定期清理"），而不是等膨胀后再解释。
- 磁盘监控必须是构建循环的强制步骤（每次构建前后检查 `docker system df` + vhdx 大小，<20GB 暂停构建先清理），而非事后补救。
- 清理范围要完整：buildx 缓存 ≠ 全部——悬空卷、悬空镜像、vhdx compact 都要纳入定期清理清单。
- 环境级操作（虚拟盘、用户目录、docker 数据）必须先征得明确同意并说明影响范围；用户说"不要写入"时必须立即停止一切写操作。
- 信任一旦因疏忽受损，只能靠透明的每一步重建——先说明、再执行、如实报告。

**预防措施清单**（后续任务强制执行）：
1. 每个构建循环结束后 `docker buildx prune -f` + `docker volume prune -f`（悬空）。
2. 构建前后检查磁盘（`docker system df` + vhdx 大小）；<20GB 暂停构建先清理。
3. vhdx 定期 compact（`wsl --shutdown` + `Optimize-VHD`/`diskpart`）。
4. 探针容器 cleanup 删除其匿名卷（或 `--rm` + 显式卷清理）。
5. >5GB 的磁盘操作（构建/拉取/批量安装）执行前向用户说明预期影响。
6. 环境级操作先征得用户明确同意并说明影响范围。
