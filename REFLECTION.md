# VesperCode 项目开发总结与反思：从模型依赖到工程治理

> **AI 协助披露：**本文的观点、经历、判断与反思由本人提供并负责；AI 仅根据本人提交的原稿协助压缩篇幅、润色语言和整理结构，没有代替本人生成实质性反思。

在本次 Agentic Engineering 实践中，我选择了 Coding Agent Harness 方向，并设计和实现了 VesperCode。项目开始时，我主要关注模型是否足够聪明、能否独立交付高质量代码。随着使用 Superpowers、Codex 和 Claude Code 构建 Harness，我逐渐意识到，真正困难的是定义目标、组织上下文、限制行为、提供确定性反馈并判断输出是否合格。

Agent 可以承担大量编码工作，但工程师的责任没有消失，而是转向目标定义、约束设计、风险判断、结果验证和最终决策。项目也暴露了我在规划、治理和工具使用上的问题。

## 规划失衡：SPEC 与 PLAN 的过度膨胀

SPEC 和 PLAN 阶段几乎占用了项目开发周期的一半，这是整个项目最明显的失衡。我经常先让 Codex 编写或重构文档，再让其他模型交叉审查。模型每次都能发现新的逻辑缝隙或边界情况，我当时错误地把“还能找到问题”等同于“还不能进入实现”，于是不断补充 Schema、失败语义、极端情况和验证规则。

这种做法确实澄清了审批绑定、候选状态、LLM 数据披露和失败恢复等概念，但代价也非常明显：文档体积迅速膨胀，上下文理解成本增加，多个合同之间高度耦合，局部修改常常引发连锁调整，真正留给实现和测试的时间被压缩。LLM 擅长发散地寻找理论漏洞，却不会自动替工程师判断某个 v1 课程项目是否值得承担这些成本。

我后来认识到，SPEC 的目标不应是穷尽所有可能性，而应是清晰界定当前版本必须实现和验收的核心行为。课程要求的陌生 Agent 冷启动测试给出了更实际的标准：文档是否足够好，不取决于它是否再也找不到可补充的细节，而取决于一个没有参与设计的 Agent 能否据此正确开始工作。以后我会为规划阶段设置明确的时间预算，用可执行性、可测试性和交付价值作为冻结条件。

## 防御的代价：治理机制的过度设计

VesperCode 的核心贡献是治理，因此安全边界、确定性验证和 Fail-Closed 机制必须落实为可独立测试的代码，而不能只写在 Prompt 中。但我一度把防御性设计推向了极端。为了避免状态混淆和旧批准被复用，我为 Snapshot、Candidate、FinalDiff、Approval、Request 和 Policy 等大量内部对象增加 SHA-256 摘要、身份绑定和完整性验证。

这些机制在跨越信任边界时很重要，但许多对象位于同一个可信控制面。对所有内部状态采用最高等级验证，使 SPEC 更难理解，也增加了实现和测试成本。真正需要重点防守的是不可逆写操作、外部披露、凭据使用和持久化写回。

防御性编程不是简单地层层加码。设计前应先画清信任边界，再根据发生概率、影响范围和可恢复性分配验证强度，否则严谨的表象可能掩盖更常见的故障。

## 决策主体：不能把 Brainstorming 交给模型

Superpowers 的 Brainstorming 能快速生成多个方案并列出优缺点，本应帮助工程师看见更完整的决策空间。但项目初期，我常常只是快速浏览方案，然后直接采纳模型标记的“Recommended”选项。从形式上看，我完成了决策流程；从实质上看，我把架构裁量权交给了模型。

模型能够列出 A、B、C，并不代表我理解了三者的本质差异和技术债务。如果重新决策，我会要求自己书面回答三个问题：为什么选择当前方案，为什么拒绝其他方案，以及当前方案最隐蔽的代价是什么。无法回答时就不应继续推进。AI 可以扩展视角，但系统失败、交付延期和长期维护的责任仍由工程师承担。

## 确定性锚点：TDD 与 Subagent

测试驱动开发是项目中最有效的实践之一。对人类而言，测试通常被视为质量防线；对 Agent 而言，失败测试、Lint 报错和类型检查结果更是下一轮修正所需的确定性反馈。它们把模糊的“实现得不对”转化为机器可观察的差异，与 VesperCode 的“动作—检查—反馈—再次行动”闭环一致。

课程要求核心机制在移除真实 LLM 后仍可通过 Mock 或 Stub 测试，这迫使我把危险动作拦截、反馈恢复和停止条件写成确定性机制，而不是期待模型自觉遵守 Prompt。

Subagent 的使用也让我重新理解任务粒度。任务过大时，Agent 容易在复杂上下文中迷失、扩张作用域或修改无关代码；任务过小时，频繁交接和重新加载上下文又会浪费大量时间。较好的任务应包含一个聚焦目标、受限文件范围、机器可观察的失败条件和清晰的完成标准。任务拆分的目的不是追求数量，而是建立可验证的责任边界。

## 基础设施盲区：Docker 与 Git Worktree

项目中最严重的现实故障之一来自 Docker。持续构建造成镜像层、Build Cache 和匿名卷堆积，WSL 虚拟盘最终膨胀到 78GB并占满系统盘，Docker daemon 崩溃，也影响了其他软件写入。讽刺的是，我在 SPEC 中花费大量精力防范低概率状态伪造，却忽视了磁盘空间、资源水位和清理策略这些高概率基础问题。

可靠性建设必须优先处理磁盘满、OOM、网络超时和服务不可用等常见故障。之后我把资源检查、容器和 volume 零残留、清理范围确认以及高影响操作的明确授权纳入流程。安全也包括避免工具以可预见的方式损害开发环境。

我对 Git Worktree 的早期理解同样停留在“多开目录并行工作”。由于没有真正理解 Worktree、Branch、Commit 和 main 指针之间的关系，项目一度出现基线混乱和错误合并风险。后来我才明白，Worktree 的核心价值不是增加并行数量，而是物理隔离任务的修改集合、基线和责任边界。Agent 可以快速执行复杂命令，但无法替工程师建立底层工具的正确心智模型；错误理解只会被自动化进一步放大。

## 从 Prompt Engineering 到 Harness Engineering

课程提出 PE 属于 CE、CE 又属于 HE，这概括了我在项目中的视角变化。早期我把大量时间用在措辞上，试图写出一条足够完整、足够严厉的 Prompt，让 Agent 一次性交付正确结果。但这种方法鲁棒性很差，模型仍可能遗漏约束或产生幻觉。

项目中后期，我不再只关注“怎么说”，而更关注“提供什么上下文、允许什么动作、如何流转、如何验收”。我开始限定每个任务可访问的文件，注入准确的 Git 状态，指定必须运行的测试，并定义遇到何种情况必须停止。实践表明，最有效的指令通常不是最长的 Prompt，而是“单一目标、最小必要上下文、明确边界和客观验收标准”的组合。工程约束比修辞更能稳定地收敛 Agent 行为。

## 如果重新开发 VesperCode

如果重新开始，我会首先为 SPEC 和 PLAN 设置不可突破的时间预算，尽早冻结并交付 MVP，而不是把“仍可优化”视为“不能实现”。其次，我会重新划分信任边界，减少可信控制面内部不必要的哈希证明，把防御集中在外部交互、危险操作和持久化写回。再次，我会在每次架构决策中保留自己的书面论证，不盲从模型推荐。最后，我会在编写应用代码前验证 Git Worktree、分支合并、Docker 存储和资源清理流程，先保证基础工程底盘可靠。

VesperCode 的开发打破了我对大模型“魔法式交付”的期待，也让我重新认识软件工程纪律。随着模型越来越擅长代码生成，工程师的核心价值不再只是编码速度，而是判断什么值得实现、哪里正在过度设计、如何为不稳定的智能建立清晰但不过度的约束，以及何时停止无休止的优化并把系统交给真实验证。对我而言，这正是 Agentic Engineering 最重要的学习成果。
